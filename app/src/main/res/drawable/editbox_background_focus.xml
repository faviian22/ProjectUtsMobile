<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/secondaryColor"/> <!-- Light brown background when focused -->
    <stroke android:width="1dp" android:color="@color/primaryLightColor"/> <!-- Darker brown border -->
</shape>
